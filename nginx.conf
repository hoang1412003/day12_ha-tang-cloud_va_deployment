events { worker_connections 1024; }
http {
    upstream agent_backend {
        server agent:8000;
    }
    server {
        listen 80;
        location / {
            proxy_pass http://agent_backend;
        }
    }
}
